# Database
DATABASE_URL="file:./dev.db"  # Local: SQLite, Production: PostgreSQL

# NextAuth
NEXTAUTH_URL="http://localhost:3001"  # Update for staging/production
NEXTAUTH_SECRET="your-nextauth-secret-here"

# Plaid API
PLAID_CLIENT_ID="your-plaid-client-id"
PLAID_SECRET="your-plaid-secret"
PLAID_ENV="sandbox"  # sandbox, development, or production

# OpenAI API Key for AI Financial Advisor
OPENAI_API_KEY="your-openai-api-key"

# Encryption Key for Sensitive Data (256-bit)
ENCRYPTION_KEY="your-256-bit-encryption-key"

# Resend Email Service
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY="your-resend-api-key"
FROM_EMAIL="onboarding@resend.dev"  # For production, use your verified domain

# Environment-specific email configuration:
# LOCAL: Uses onboarding@resend.dev (can only send to verified email in free tier)
# STAGING: Uses onboarding@resend.dev (can only send to verified email in free tier)
# PRODUCTION: Should use your verified domain email (e.g., noreply@yourdomain.com)